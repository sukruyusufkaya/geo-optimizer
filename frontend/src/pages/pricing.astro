---
import Shell from '../components/Shell.astro';
import PricingObjectionSurvey from '../components/PricingObjectionSurvey';

const selfServePlans = [
  {
    id: 'free',
    name: 'Free',
    price: '$0',
    period: 'forever',
    status: 'Available now',
    statusStyle: 'bg-pass-wash text-pass-deep',
    description: 'Single-URL audit with a full GEO score and the complete 8-category breakdown. No account required.',
    cta: 'Start free audit',
    ctaHref: '/',
    ctaStyle: 'border border-rail bg-white text-ink hover:bg-pass-wash',
    recommended: false,
    cadence: 'run: on demand · 5/day',
    features: [
      '5 audits per day',
      'GEO score 0–100',
      'Full 8-category GEO report',
      'Actionable recommendations',
      'CLI (MIT, unlimited)',
    ],
  },
  {
    id: 'pro',
    name: 'Pro',
    price: '$19',
    period: '/month',
    status: 'Founding price',
    statusStyle: 'bg-pass-wash text-pass-deep',
    description: 'Weekly monitoring for one domain, score history, regression alerts, PDF export, and API access. For developers and consultants managing one site.',
    cta: 'Start with Pro',
    ctaHref: 'https://app.geoready.dev/signup?plan=pro&intent=monitoring&onboarding=first_domain&utm_source=pricing&utm_medium=plan_card&utm_campaign=pricing_to_paid&utm_content=pro_card',
    ctaStyle: 'bg-pass-deep text-white hover:bg-pass',
    recommended: true,
    cadence: 'schedule: weekly · 1 domain',
    features: [
      'Unlimited audits',
      '1 domain — weekly monitoring',
      '30-audit score history',
      'Regression alerts (when your score moves by 5 points or more)',
      'PDF export',
      'API access (Bearer token)',
    ],
  },
  {
    id: 'studio',
    name: 'Studio',
    price: '$49',
    period: '/month',
    status: 'Founding price',
    statusStyle: 'bg-pass-wash text-pass-deep',
    description: 'AI citation tracking, multi-site daily monitoring, unbranded PDF exports, and competitor comparison. For consultants and agencies managing a portfolio.',
    cta: 'Start with Studio',
    ctaHref: 'https://app.geoready.dev/signup?plan=studio&intent=citations&onboarding=first_domain&utm_source=pricing&utm_medium=plan_card&utm_campaign=pricing_to_paid&utm_content=studio_card',
    ctaStyle: 'border border-rail bg-white text-ink hover:bg-pass-wash',
    recommended: false,
    cadence: 'schedule: daily · 15 domains',
    features: [
      'Everything in Pro',
      'AI citation tracking — 5 automated checks/day (Perplexity)',
      '15 domains — daily monitoring',
      '12-month score history',
      'Unbranded PDF export (removes GeoReady footer and badge)',
      'Custom-logo white-label: roadmap',
      'Competitor comparison',
      'Batch audit — 50 URLs',
      'Priority support',
    ],
  },
];

const scalePlans = [
  {
    id: 'agency',
    name: 'Agency',
    price: '$89',
    period: '/month',
    status: 'Founding price',
    statusStyle: 'bg-pass-wash text-pass-deep',
    description: 'Team workspace, 50-domain daily portfolio monitoring, AI answer snapshots, and unlimited batch audits.',
    cta: 'Start with Agency',
    ctaHref: 'https://app.geoready.dev/signup?plan=agency&intent=portfolio&onboarding=first_domain&utm_source=pricing&utm_medium=plan_card&utm_campaign=pricing_to_paid&utm_content=agency_card',
    cadence: 'schedule: daily · 50 domains',
    features: [
      'Everything in Studio',
      '50 domains — daily monitoring',
      '24-month score history',
      'Team workspace (owner / editor / viewer)',
      'Unlimited batch audits',
      'AI answer snapshots (Perplexity automated; ChatGPT and Google via manual paste)',
      'Citation quality scoring',
    ],
  },
  {
    id: 'enterprise',
    name: 'Enterprise',
    price: 'Custom',
    period: '',
    status: 'Talk to us',
    statusStyle: 'border border-rail text-ink-mute',
    description: 'SLA, dedicated account manager, on-premise deployment, hourly monitoring, and custom integrations.',
    // No sales team today — honest label + direct email to the maintainer
    // (address already public on the privacy policy page).
    cta: 'Contact us',
    ctaHref: 'mailto:juancamilo.auriti@gmail.com?subject=GeoReady%20Enterprise',
    cadence: 'schedule: hourly · unlimited',
    features: [
      'Everything in Agency',
      'Unlimited domains',
      'Hourly monitoring',
      'SLA',
      'On-premise deployment',
      'Webhooks / Slack / CMS integrations',
      'Dedicated account manager',
      'Custom onboarding',
    ],
  },
];

const planChoiceRows = [
  {
    intent: 'I need one current GEO score before I change anything.',
    plan: 'Free audit',
    reason: 'Use the free audit when you need a one-time diagnosis for one URL, with the full 8-category report and no account.',
    href: '/#audit-form',
    cta: 'Run free audit',
    tracking: 'pricing_choice_free_audit',
    planId: '',
    planName: '',
    price: '',
    period: '',
  },
  {
    intent: 'I need to know when my site gets worse or better.',
    plan: 'Pro',
    reason: 'Choose Pro when one domain needs weekly monitoring, score history, regression alerts, PDF export, and API access.',
    href: 'https://app.geoready.dev/signup?plan=pro&intent=monitoring&onboarding=first_domain&utm_source=pricing&utm_medium=plan_choice&utm_campaign=pricing_to_paid&utm_content=pro_choice',
    cta: 'Start Pro',
    tracking: 'pricing_choice_pro_monitoring',
    planId: 'pro',
    planName: 'Pro',
    price: '$19',
    period: 'monthly',
  },
  {
    intent: 'I need AI citation tracking or client-ready reports.',
    plan: 'Studio',
    reason: 'Choose Studio when AI citations, competitor comparison, unbranded exports, or several monitored domains matter.',
    href: 'https://app.geoready.dev/signup?plan=studio&intent=citations&onboarding=first_domain&utm_source=pricing&utm_medium=plan_choice&utm_campaign=pricing_to_paid&utm_content=studio_choice',
    cta: 'Start Studio',
    tracking: 'pricing_choice_studio_citations',
    planId: 'studio',
    planName: 'Studio',
    price: '$49',
    period: 'monthly',
  },
  {
    intent: 'I manage a portfolio and need team or bulk workflows.',
    plan: 'Agency',
    reason: 'Choose Agency when 50 monitored domains, team access, unlimited batch audits, and AI answer snapshots are part of the workflow.',
    href: 'https://app.geoready.dev/signup?plan=agency&intent=portfolio&onboarding=first_domain&utm_source=pricing&utm_medium=plan_choice&utm_campaign=pricing_to_paid&utm_content=agency_choice',
    cta: 'Start Agency',
    tracking: 'pricing_choice_agency_portfolio',
    planId: 'agency',
    planName: 'Agency',
    price: '$89',
    period: 'monthly',
  },
] as const;

const afterCheckoutSteps = [
  {
    step: '01',
    title: 'Stripe checkout',
    desc: 'Payment is handled by Stripe. GeoReady does not store card details on its own servers.',
  },
  {
    step: '02',
    title: 'Plan activates immediately',
    desc: 'After payment, the selected plan is available in the app without a sales call or manual approval.',
  },
  {
    step: '03',
    title: 'Claim or add your domain',
    desc: 'If you start from a free report, the claim link carries that report into your account. Pricing links open the app with the first-domain onboarding context.',
  },
  {
    step: '04',
    title: 'Monitoring starts from there',
    desc: 'Pro runs weekly for one domain. Studio and Agency move to daily multi-site monitoring with longer history and client workflows.',
  },
] as const;

const trustAssurances = [
  ['Free stays useful', 'The free audit remains a complete 8-category report with no account and no card.'],
  ['Cancel anytime', 'There is no annual lock-in or sales contract for self-serve plans.'],
  ['Open methodology', 'The core audit engine, scoring logic, and CLI are MIT-licensed and auditable.'],
  ['Founding price lock', 'The founding price stays locked while the subscription remains active.'],
] as const;

// '✓' renders as a drawn SVG check; '—' stays typographic.
const comparisonRows: [string, string, string, string, string][] = [
  ['GEO score (0–100)',      '✓',       '✓',        '✓',        '✓'],
  ['Full 8-category report', '✓',       '✓',        '✓',        '✓'],
  ['Audits per day',         '5',       '∞',        '∞',        '∞'],
  ['Domain monitoring',      '—',       '1 / week', '15 / day', '50 / day'],
  ['Score history',          '—',       '30 audits','12 months','24 months'],
  ['Regression alerts',      '—',       '✓',        '✓',        '✓'],
  ['PDF export',             '—',       '✓',        '✓',        '✓'],
  ['Unbranded PDF export',   '—',       '—',        '✓',        '✓'],
  ['Custom-logo white-label','—',       '—',        'roadmap',  'roadmap'],
  ['Competitor comparison',  '—',       '—',        '✓',        '✓'],
  ['Bulk audit',             '—',       '—',        '50 URLs',  '∞'],
  ['Team workspace',         '—',       '—',        '—',        '✓'],
  ['AI search snapshots',    '—',       '—',        '5 runs/day', '✓'],
  ['API access',             '—',       '✓',        '✓',        '✓'],
  ['CLI (MIT)',               '✓',       '✓',        '✓',        '✓'],
];

const faqs = [
  {
    q: 'Is the free audit really free?',
    a: 'Yes. The web audit requires no account, no credit card, and no signup. You get a full GEO score and the complete 8-category breakdown immediately. The CLI is MIT-licensed and free to use without limits.',
  },
  {
    q: 'Do I need to register to use the free audit?',
    a: 'No. Just enter a URL and run the audit. No email is required to see your results.',
  },
  {
    q: 'What does Pro add that Free does not?',
    a: 'Pro adds continuity, not more report. The free audit is a one-shot snapshot with all 8 categories. Pro monitors your domain weekly, tracks score history over time, and sends alerts when your GEO score drops. It also includes PDF export and API access.',
  },
  {
    q: 'Are paid plans available right now?',
    a: 'Yes. Pick a plan, create an account, and pay — your subscription is active immediately. No sales call, no waitlist. Payments are processed securely by Stripe.',
  },
  {
    q: 'What happens after I choose a paid plan?',
    a: 'You complete Stripe checkout, return to the GeoReady app, and the selected plan activates immediately. If you came from a free report, the claim link can attach that report to your account; otherwise you add a domain from the dashboard and monitoring starts from that point.',
  },
  {
    q: 'What is the founding price?',
    a: 'The current prices are a founding rate for early members. They are locked for the life of the subscription as long as it stays active. The public list price will be higher later.',
  },
  {
    q: 'Will the CLI remain open source?',
    a: 'Yes. The CLI (geo-optimizer-skill on PyPI) is MIT-licensed and will remain free forever. SaaS plans add server-side continuity features the CLI cannot provide on its own.',
  },
  {
    q: 'Is there a free trial for paid plans?',
    a: 'There is no time-limited trial. The Free plan is the permanent baseline — it returns real audit results without commitment. Paid plans add monitoring and team features on top.',
  },
  {
    q: 'Can I cancel at any time?',
    a: 'Yes. You can cancel from your account settings at any time, with no lock-in or penalties.',
  },
];

const heroCadenceRows = [
  { plan: 'Free', value: 'run: on demand', detail: '5 audits/day' },
  { plan: 'Pro', value: 'schedule: weekly', detail: '1 domain' },
  { plan: 'Studio', value: 'schedule: daily', detail: '15 domains' },
  { plan: 'Agency', value: 'schedule: daily', detail: '50 domains' },
] as const;
---

<Shell
  title="GeoReady Pricing — Free AI Audit, Pro Monitoring & Alerts"
  description="The full 8-category GEO audit is free, no account required. Pro adds weekly monitoring, score history, and alerts. Studio and Agency add multi-site daily monitoring and unbranded exports."
  canonical="https://geoready.dev/pricing/"
>
  {/* Hero */}
  <section class="bg-paper">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-20">
      <div class="grid items-start gap-10 lg:grid-cols-[minmax(0,1fr)_minmax(340px,0.52fr)] lg:gap-14">
        <div>
          <h1 class="max-w-3xl font-head text-4xl font-extrabold leading-[1.05] tracking-[-0.03em] text-ink md:text-5xl">
            Free GEO audit, forever. Pay only when you need monitoring.
          </h1>
          <p class="mt-5 max-w-2xl text-base leading-relaxed text-ink-soft">
            Run a full GEO score on any URL with no account and no card — every time you want.
            Paid plans add what a one-shot audit can't: weekly monitoring, score history, regression
            alerts, exports, and team workflows.
          </p>

          <div class="mt-7 flex flex-col gap-3 sm:flex-row sm:items-center">
            <a
              href="/"
              data-cta="pricing_hero_free_audit"
              class="inline-flex items-center justify-center gap-2 rounded-[4px] bg-pass-deep px-6 py-3 font-mono text-[12px] font-semibold uppercase tracking-[0.12em] text-white transition-colors hover:bg-pass"
            >
              Start free audit
              <svg class="h-3 w-3" viewBox="0 0 12 12" fill="currentColor" aria-hidden="true">
                <path d="M3 1.5 9.5 6 3 10.5V1.5Z" />
              </svg>
            </a>
            <span class="text-sm text-ink-mute">
              No time trial. No card required for Free. Cancel anytime.
            </span>
          </div>

          <div class="mt-8 grid gap-3 sm:grid-cols-3">
            <div class="border-l border-rail pl-4">
              <p class="text-sm font-semibold text-ink">Free</p>
              <p class="mt-1 text-sm text-ink-soft">Single-URL audit, no account.</p>
            </div>
            <div class="border-l border-rail pl-4">
              <p class="text-sm font-semibold text-ink">Pro</p>
              <p class="mt-1 text-sm text-ink-soft">$19/month founding price.</p>
            </div>
            <div class="border-l border-rail pl-4">
              <p class="text-sm font-semibold text-ink">Studio</p>
              <p class="mt-1 text-sm text-ink-soft">Multi-site monitoring and exports.</p>
            </div>
          </div>

          <p class="mt-8 max-w-2xl text-sm leading-relaxed text-ink-mute">
            The web audit and CLI are MIT-licensed and remain free forever. The CLI runs locally with
            no usage limits.
          </p>
        </div>

        {/* Monitoring cadence — the plan facts as a ledger, not a mockup */}
        <aside class="rounded-[4px] border border-rail bg-white" aria-label="Monitoring cadence by plan">
          <div class="flex items-center justify-between border-b border-rail px-5 py-2.5">
            <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">monitoring cadence</span>
            <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-pass-deep">per plan</span>
          </div>
          <ul class="divide-y divide-rail">
            {heroCadenceRows.map((row) => (
              <li class="flex items-baseline justify-between gap-4 px-5 py-3.5">
                <span class="text-sm font-semibold text-ink">{row.plan}</span>
                <span class="text-right font-mono text-[12px] text-ink-soft">
                  {row.value} <span class="text-ink-mute">· {row.detail}</span>
                </span>
              </li>
            ))}
          </ul>
          <div class="border-t border-rail px-5 py-2.5">
            <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">billing: Stripe · cancel anytime</span>
          </div>
        </aside>
      </div>

      <div class="mt-12 grid gap-px overflow-hidden rounded-[4px] border border-rail bg-rail md:grid-cols-4">
        {[
          ['Free', 'Audit once, learn what to fix.'],
          ['Pro', 'Track one domain weekly.'],
          ['Studio', 'Run a portfolio daily.'],
          ['Agency', 'Add team and bulk workflows.'],
        ].map(([label, text]) => (
          <div class="bg-white p-5">
            <p class="text-sm font-semibold text-ink">{label}</p>
            <p class="mt-1.5 text-sm leading-relaxed text-ink-soft">{text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* Credibility row — verifiable signals only (open-source, research-backed, coverage) */}
  <section aria-label="What the audit is built on" class="border-t border-rail bg-white">
    <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6">
      <ul class="grid gap-px overflow-hidden rounded-[4px] border border-rail bg-rail sm:grid-cols-2 lg:grid-cols-4">
        {[
          ['Open source, MIT', 'Scoring weights are public and auditable on GitHub.', 'github.com/Auriti-Labs'],
          ['Research-grounded', 'Weights follow Princeton KDD 2024 and AutoGEO ICLR 2026.', 'KDD 2024 · ICLR 2026'],
          ['27 AI bots checked', 'robots.txt access verified across 27 AI crawlers.', 'models/config.py · AI_BOTS'],
          ['47 citability methods', 'Each audit runs 47 citation-signal checks.', 'core/citability.py'],
        ].map(([label, text, source]) => (
          <li class="bg-paper p-5">
            <p class="text-sm font-semibold text-ink">{label}</p>
            <p class="mt-1.5 text-sm leading-relaxed text-ink-soft">{text}</p>
            <p class="mt-3 font-mono text-[10px] uppercase tracking-[0.1em] text-ink-mute">{source}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  {/* Plan cards */}
  <section aria-labelledby="plans-heading" class="border-t border-rail bg-paper">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <h2 id="plans-heading" class="sr-only">Plans</h2>

      <p class="mb-6 font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">
        Founding price for early members — locked for life while your subscription stays active.
      </p>

      {/* Self-serve row */}
      <div class="mb-4 grid grid-cols-1 gap-4 md:grid-cols-3">
        {selfServePlans.map((plan) => (
          <div class={`flex flex-col rounded-[4px] border ${plan.recommended ? 'border-pass/50 bg-pass-wash/60' : 'border-rail bg-white'}`}>
            <div class={`flex items-center justify-between border-b px-5 py-2.5 ${plan.recommended ? 'border-pass/30' : 'border-rail'}`}>
              <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">{plan.cadence}</span>
              {plan.recommended && (
                <span class="rounded-[2px] bg-pass-deep px-1.5 py-0.5 font-mono text-[10px] font-semibold uppercase tracking-[0.1em] text-white">
                  Recommended
                </span>
              )}
            </div>

            <div class="flex flex-1 flex-col p-5">
              <div class="mb-3 flex items-center justify-between">
                <span class="text-sm font-semibold text-ink">{plan.name}</span>
                <span class={`rounded-[2px] px-2 py-0.5 font-mono text-[10px] uppercase tracking-[0.1em] ${plan.statusStyle}`}>
                  {plan.status}
                </span>
              </div>
              <div class="flex items-baseline gap-1">
                <span class="font-head text-3xl font-extrabold tracking-[-0.02em] text-ink">{plan.price}</span>
                {plan.period && <span class="font-mono text-xs text-ink-mute">{plan.period}</span>}
              </div>
              <p class="mt-3 text-sm leading-relaxed text-ink-soft">{plan.description}</p>

              <ul class="mb-6 mt-4 flex-1 space-y-2">
                {plan.features.map((f) => (
                  <li class="flex items-start gap-2 text-sm text-ink-soft">
                    <svg aria-hidden="true" class="mt-0.5 h-3.5 w-3.5 shrink-0 text-pass-deep" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    {f}
                  </li>
                ))}
              </ul>

              <a
                href={plan.ctaHref}
                data-plan-id={plan.id}
                data-plan-name={plan.name}
                data-plan-price={plan.price}
                data-plan-period={plan.period}
                data-plan-currency="USD"
                data-cta-location="pricing_page"
                class={`block rounded-[4px] px-4 py-2.5 text-center font-mono text-[12px] font-semibold uppercase tracking-[0.12em] transition-colors ${plan.ctaStyle}`}
              >
                {plan.cta}
              </a>
            </div>
          </div>
        ))}
      </div>

      {/* Scale row */}
      <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
        {scalePlans.map((plan) => (
          <div class="flex flex-col rounded-[4px] border border-rail bg-white">
            <div class="flex items-center justify-between border-b border-rail px-5 py-2.5">
              <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">{plan.cadence}</span>
            </div>
            <div class="flex flex-1 flex-col p-5">
              <div class="mb-3 flex items-center justify-between">
                <span class="text-sm font-semibold text-ink">{plan.name}</span>
                <span class={`rounded-[2px] px-2 py-0.5 font-mono text-[10px] uppercase tracking-[0.1em] ${plan.statusStyle}`}>
                  {plan.status}
                </span>
              </div>
              <div class="flex items-baseline gap-1">
                <span class="font-head text-3xl font-extrabold tracking-[-0.02em] text-ink">{plan.price}</span>
                {plan.period && <span class="font-mono text-xs text-ink-mute">{plan.period}</span>}
              </div>
              <p class="mt-3 text-sm leading-relaxed text-ink-soft">{plan.description}</p>

              <ul class="mb-6 mt-4 flex-1 space-y-2">
                {plan.features.map((f) => (
                  <li class="flex items-start gap-2 text-sm text-ink-soft">
                    <svg aria-hidden="true" class="mt-0.5 h-3.5 w-3.5 shrink-0 text-pass-deep" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    {f}
                  </li>
                ))}
              </ul>

              {/* Enterprise è lead-gen ("Contact us" via mailto), non selezione piano self-serve:
                  usa data-cta → geo_cta_clicked, non data-plan-id → geo_plan_selected.
                  Il nome evento resta "pricing_enterprise_contact_sales" per continuità storica GA4. */}
              {plan.id === 'enterprise' ? (
                <a
                  href={plan.ctaHref}
                  data-cta="pricing_enterprise_contact_sales"
                  class="block rounded-[4px] border border-rail bg-white px-4 py-2.5 text-center font-mono text-[12px] font-semibold uppercase tracking-[0.12em] text-ink transition-colors hover:bg-pass-wash"
                >
                  {plan.cta}
                </a>
              ) : (
                <a
                  href={plan.ctaHref}
                  data-plan-id={plan.id}
                  data-plan-name={plan.name}
                  data-plan-price={plan.price}
                  data-plan-period={plan.period}
                  data-plan-currency="USD"
                  data-cta-location="pricing_page"
                  class="block rounded-[4px] border border-rail bg-white px-4 py-2.5 text-center font-mono text-[12px] font-semibold uppercase tracking-[0.12em] text-ink transition-colors hover:bg-pass-wash"
                >
                  {plan.cta}
                </a>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* After checkout */}
  <section class="border-t border-rail bg-white" aria-labelledby="after-checkout-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <div class="grid gap-10 lg:grid-cols-[minmax(0,1fr)_minmax(320px,0.5fr)]">
        <div>
          <div class="flex flex-wrap items-baseline justify-between gap-x-6 gap-y-2">
            <h2 id="after-checkout-heading" class="font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
              What happens after you pick a paid plan
            </h2>
            <p class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">checkout: Stripe</p>
          </div>
          <p class="mt-3 max-w-2xl text-sm leading-relaxed text-ink-soft">
            The next screen is not a vague "contact sales" handoff. Self-serve plans activate through
            Stripe, then the app guides you to claim the report or add the first monitored domain.
          </p>

          <ol class="mt-6 grid gap-px overflow-hidden rounded-[4px] border border-rail bg-rail sm:grid-cols-2">
            {afterCheckoutSteps.map((item) => (
              <li class="bg-paper p-5">
                <p class="font-mono text-[11px] font-semibold uppercase tracking-[0.14em] text-pass-deep">{item.step}</p>
                <h3 class="mt-2 text-sm font-semibold text-ink">{item.title}</h3>
                <p class="mt-1 text-sm leading-relaxed text-ink-soft">{item.desc}</p>
              </li>
            ))}
          </ol>
        </div>

        <aside class="rounded-[4px] border border-rail bg-paper" aria-label="Buyer checks">
          <div class="border-b border-rail px-5 py-2.5">
            <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">buyer checks</span>
          </div>
          <ul class="divide-y divide-rail">
            {trustAssurances.map(([label, text]) => (
              <li class="px-5 py-4">
                <p class="text-sm font-semibold text-ink">{label}</p>
                <p class="mt-1 text-sm leading-relaxed text-ink-soft">{text}</p>
              </li>
            ))}
          </ul>
          <div class="border-t border-rail px-5 py-4">
            <a
              href="/report/demo/"
              data-cta="pricing_after_checkout_demo_report"
              class="inline-flex rounded-[4px] border border-rail bg-white px-4 py-2.5 font-mono text-[12px] font-semibold uppercase tracking-[0.12em] text-ink transition-colors hover:bg-pass-wash"
            >
              View a demo report
            </a>
          </div>
        </aside>
      </div>
    </div>
  </section>

  {/* Plan chooser */}
  <section class="border-t border-rail bg-paper" aria-labelledby="plan-chooser-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <div class="max-w-3xl">
        <h2 id="plan-chooser-heading" class="font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
          Which GeoReady plan should you choose?
        </h2>
        <p class="mt-3 text-sm leading-relaxed text-ink-soft">
          Pick the plan by the job after the audit, not by feature count. A one-time
          diagnosis stays free. Ongoing monitoring starts with Pro. AI citation
          tracking and client workflows start with Studio.
        </p>
      </div>

      <div class="mt-6 overflow-x-auto">
        <table class="min-w-full border-collapse overflow-hidden rounded-[4px] border border-rail bg-white text-left text-sm">
          <thead class="bg-paper">
            <tr>
              <th scope="col" class="border-b border-rail px-4 py-3 font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">If your next job is...</th>
              <th scope="col" class="border-b border-rail px-4 py-3 font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Choose</th>
              <th scope="col" class="border-b border-rail px-4 py-3 font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Why</th>
              <th scope="col" class="border-b border-rail px-4 py-3 font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Action</th>
            </tr>
          </thead>
          <tbody>
            {planChoiceRows.map((row) => (
              <tr>
                <th scope="row" class="border-b border-rail px-4 py-3 text-sm font-semibold text-ink">
                  {row.intent}
                </th>
                <td class="border-b border-rail px-4 py-3 font-mono text-xs text-pass-deep">
                  {row.plan}
                </td>
                <td class="border-b border-rail px-4 py-3 text-ink-soft">
                  {row.reason}
                </td>
                <td class="border-b border-rail px-4 py-3">
                  {row.planId ? (
                    <a
                      href={row.href}
                      data-plan-id={row.planId}
                      data-plan-name={row.planName}
                      data-plan-price={row.price}
                      data-plan-period={row.period}
                      data-plan-currency="USD"
                      data-cta-location={row.tracking}
                      class="inline-flex whitespace-nowrap rounded-[4px] border border-rail bg-white px-3 py-2 font-mono text-[11px] font-semibold uppercase tracking-[0.1em] text-ink transition-colors hover:bg-pass-wash"
                    >
                      {row.cta}
                    </a>
                  ) : (
                    <a
                      href={row.href}
                      data-cta={row.tracking}
                      class="inline-flex whitespace-nowrap rounded-[4px] border border-rail bg-white px-3 py-2 font-mono text-[11px] font-semibold uppercase tracking-[0.1em] text-ink transition-colors hover:bg-pass-wash"
                    >
                      {row.cta}
                    </a>
                  )}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </section>

  {/* Free vs Pro */}
  <section class="border-t border-rail bg-white" aria-labelledby="free-vs-pro-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <h2 id="free-vs-pro-heading" class="font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
        Free vs Pro — what changes
      </h2>
      <p class="mb-8 mt-2 max-w-2xl text-sm text-ink-soft">
        The free audit is a single-URL, point-in-time snapshot. Pro turns it into an ongoing signal.
      </p>
      <div class="grid gap-6 md:grid-cols-2">
        <div class="rounded-[4px] border border-rail bg-paper">
          <div class="flex items-center justify-between border-b border-rail px-5 py-2.5">
            <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">Free</span>
            <span class="rounded-[2px] border border-rail px-1.5 py-0.5 font-mono text-[10px] uppercase tracking-[0.1em] text-ink-mute">available now</span>
          </div>
          <ul class="space-y-3 p-5 text-sm text-ink-soft">
            {[
              'Full GEO score (0–100) for any public URL',
              'All 8 category breakdowns, no email required',
              'Actionable recommendations',
              'Up to 5 audits per day, no account needed',
              'CLI unlimited via pip (MIT license, forever)',
            ].map((f) => (
              <li class="flex items-start gap-2.5">
                <svg aria-hidden="true" class="mt-0.5 h-3.5 w-3.5 shrink-0 text-pass-deep" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="20 6 9 17 4 12"/>
                </svg>
                {f}
              </li>
            ))}
          </ul>
        </div>
        <div class="rounded-[4px] border border-pass/50 bg-pass-wash/60">
          <div class="flex items-center justify-between border-b border-pass/30 px-5 py-2.5">
            <span class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">Pro</span>
            <span class="rounded-[2px] bg-pass-wash px-1.5 py-0.5 font-mono text-[10px] uppercase tracking-[0.1em] text-pass-deep">founding price</span>
          </div>
          {/* Diff notation: '+' marks what Pro adds on top of the Free baseline. */}
          <ul class="space-y-3 p-5 text-sm text-ink-soft">
            {[
              'Weekly monitoring — score tracked automatically',
              'Score history — 30 audits, see trends over time',
              'Regression alerts — notified when your score moves by 5 points or more',
              'PDF export + API access (Bearer token)',
            ].map((f) => (
              <li class="flex items-start gap-2.5">
                <span aria-hidden="true" class="mt-0.5 shrink-0 font-mono text-[13px] font-semibold leading-none text-pass-deep">+</span>
                {f}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  {/* Product screenshots — real dashboard, real data (captured 2026-07-15) */}
  <section class="border-t border-rail bg-paper" aria-labelledby="product-shots-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <div class="flex flex-wrap items-baseline justify-between gap-x-6 gap-y-2">
        <h2 id="product-shots-heading" class="font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
          This is what you get
        </h2>
        <p class="font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">captured: 2026-07-15 · app.geoready.dev</p>
      </div>
      <p class="mb-8 mt-2 max-w-2xl text-sm text-ink-soft">
        Real screenshots from the live platform — the same dashboard, monitoring and alerts you get on
        every paid plan. No mockups.
      </p>
      <div class="grid gap-6">
        <figure class="overflow-hidden rounded-[4px] border border-rail bg-white">
          <img
            src="/assets/screenshots/app-dashboard.webp"
            alt="GeoReady dashboard: GEO score 88 with score trend across 30 saved audits, signals passing 16 of 18, citations and coverage tiles"
            width="1600"
            height="1000"
            loading="lazy"
            decoding="async"
            class="h-auto w-full"
          />
          <figcaption class="border-t border-rail px-5 py-3 text-xs text-ink-mute">
            Dashboard — current score, trend across saved audits, and the next fixes in one place.
          </figcaption>
        </figure>
        <div class="grid gap-6 md:grid-cols-2">
          <figure class="overflow-hidden rounded-[4px] border border-rail bg-white">
            <img
              src="/assets/screenshots/app-domains-monitoring.webp"
              alt="GeoReady domains page: tracked domain with automatic monitoring enabled, last audit date and score history actions"
              width="1600"
              height="1000"
              loading="lazy"
              decoding="async"
              class="h-auto w-full"
            />
            <figcaption class="border-t border-rail px-5 py-3 text-xs text-ink-mute">
              Domain monitoring — audits re-run automatically, score history per domain.
            </figcaption>
          </figure>
          <figure class="overflow-hidden rounded-[4px] border border-rail bg-white">
            <img
              src="/assets/screenshots/app-regression-alerts.webp"
              alt="GeoReady regression alerts page: monitoring active, alerts trigger on score drops of 5 or more points, llms.txt changes and AI bot blocking"
              width="1600"
              height="1000"
              loading="lazy"
              decoding="async"
              class="h-auto w-full"
            />
            <figcaption class="border-t border-rail px-5 py-3 text-xs text-ink-mute">
              Regression alerts — score drops ≥5 pts, llms.txt changes and bot blocking trigger a notification.
            </figcaption>
          </figure>
        </div>
      </div>
    </div>
  </section>

  {/* Why not a traditional SEO tool? */}
  <section class="border-t border-rail bg-white" aria-labelledby="geo-vs-seo-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <h2 id="geo-vs-seo-heading" class="font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
        Why not a traditional SEO tool?
      </h2>
      <p class="mb-8 mt-2 max-w-2xl text-sm text-ink-soft">
        SEO tools measure ranking signals. GeoReady measures citation signals — a different question entirely.
      </p>
      <div class="grid gap-px overflow-hidden rounded-[4px] border border-rail bg-rail sm:grid-cols-2 lg:grid-cols-3">
        <div class="bg-paper p-5">
          <p class="mb-2 text-sm font-semibold text-ink">Ranking vs. being cited</p>
          <p class="text-sm leading-relaxed text-ink-soft">
            Traditional SEO predicts whether Google ranks your page. GEO predicts whether ChatGPT,
            Perplexity, or Claude cites your content in an answer. These are correlated but not the same.
          </p>
        </div>
        <div class="bg-paper p-5">
          <p class="mb-2 text-sm font-semibold text-ink">Different signal set</p>
          <p class="text-sm leading-relaxed text-ink-soft">
            GEO audits llms.txt, ai.txt, JSON-LD depth, factual density, brand entity coherence,
            and AI-crawler permissions — signals that SEO tools don't surface.
          </p>
        </div>
        <div class="bg-paper p-5">
          <p class="mb-2 text-sm font-semibold text-ink">Research-backed scoring</p>
          <p class="text-sm leading-relaxed text-ink-soft">
            Scoring weights are grounded in <a href="/research/" class="text-pass-deep underline decoration-rail underline-offset-4 hover:decoration-pass-deep">Princeton KDD 2024 and AutoGEO ICLR 2026</a> research
            on what actually influences AI search retrieval.
          </p>
        </div>
        <div class="bg-paper p-5">
          <p class="mb-2 text-sm font-semibold text-ink">Open source and auditable</p>
          <p class="text-sm leading-relaxed text-ink-soft">
            Every scoring rule is readable in the <a href="https://github.com/Auriti-Labs/geo-optimizer-skill" target="_blank" rel="noopener" class="text-pass-deep underline decoration-rail underline-offset-4 hover:decoration-pass-deep">public repository</a>.
            No black box — you can audit the auditor.
          </p>
        </div>
        <div class="bg-paper p-5">
          <p class="mb-2 text-sm font-semibold text-ink">Forward-looking signals</p>
          <p class="text-sm leading-relaxed text-ink-soft">
            llms.txt and ai.txt are emerging standards. GEO tracks adoption curves and updates scoring
            weights as the AI search landscape evolves.
          </p>
        </div>
        <div class="bg-paper p-5">
          <p class="mb-2 text-sm font-semibold text-ink">Complements, doesn't replace</p>
          <p class="text-sm leading-relaxed text-ink-soft">
            Use GeoReady alongside your existing SEO tooling. A strong GEO score makes a well-ranked
            page more likely to be cited by AI assistants.
          </p>
        </div>
      </div>
    </div>
  </section>

  {/* Feature comparison table */}
  <section class="border-t border-rail bg-paper" aria-labelledby="comparison-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <h2 id="comparison-heading" class="font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
        Feature comparison
      </h2>
      <p class="mb-8 mt-2 text-sm text-ink-soft">
        Items marked "roadmap" are planned and not yet available. Everything else is live today.
      </p>
      <div class="overflow-x-auto">
        <table class="w-full min-w-[480px] border-collapse overflow-hidden rounded-[4px] border border-rail bg-white text-sm">
          <thead class="bg-paper">
            <tr>
              <th class="w-[40%] border-b border-rail px-4 py-3 text-left font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Feature</th>
              <th class="border-b border-rail px-3 py-3 text-center font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Free</th>
              <th class="border-b border-rail bg-pass-wash/60 px-3 py-3 text-center font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-pass-deep">Pro</th>
              <th class="border-b border-rail px-3 py-3 text-center font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Studio</th>
              <th class="border-b border-rail px-3 py-3 text-center font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">Agency</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-rail">
            {comparisonRows.map(([feature, ...cells]) => (
              <tr class="transition-colors hover:bg-pass-wash/30">
                <td class="px-4 py-2.5 text-ink-soft">{feature}</td>
                {cells.map((value, i) => (
                  <td class={`px-3 py-2.5 text-center font-mono text-xs tabular-nums ${i === 1 ? 'bg-pass-wash/40' : ''} ${value === '—' ? 'text-rail' : 'text-ink-soft'}`}>
                    {value === '✓' ? (
                      <svg aria-label="Included" role="img" class="inline h-3.5 w-3.5 text-pass-deep" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="20 6 9 17 4 12"/>
                      </svg>
                    ) : (
                      value
                    )}
                  </td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <p class="mt-3 font-mono text-[11px] uppercase tracking-[0.12em] text-ink-mute">
        source: plan entitlements · billed via Stripe
      </p>
    </div>
  </section>

  {/* FAQ */}
  <section class="border-t border-rail bg-white" aria-labelledby="faq-heading">
    <div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-16">
      <h2 id="faq-heading" class="mb-8 font-head text-2xl font-extrabold tracking-[-0.02em] text-ink md:text-3xl">
        Frequently asked questions
      </h2>
      <div class="grid gap-x-12 gap-y-8 md:grid-cols-2">
        {faqs.map((faq) => (
          <div>
            <p class="mb-2 text-sm font-semibold text-ink">{faq.q}</p>
            <p class="text-sm leading-relaxed text-ink-soft">{faq.a}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* Footer CTA — the page's verdict band */}
  <section class="border-t border-rail bg-pass-deep">
    <div class="mx-auto max-w-7xl px-4 py-14 text-center sm:px-6 md:py-16">
      <div class="mx-auto flex max-w-3xl flex-wrap items-baseline justify-center gap-x-6 gap-y-2">
        <h2 class="font-head text-2xl font-extrabold tracking-[-0.02em] text-white md:text-3xl">
          Start with a free audit. Upgrade when you are ready.
        </h2>
      </div>
      <p class="mx-auto mb-6 mt-3 max-w-md text-sm leading-relaxed text-pass-wash/90">
        No account required for the audit. Paid plans are live now at a founding price, locked for life while your subscription stays active. Cancel anytime.
      </p>
      <div class="flex flex-col justify-center gap-3 sm:flex-row">
        <a
          href="/"
          data-cta="pricing_footer_free_audit"
          class="inline-block rounded-[4px] border border-white/40 px-6 py-2.5 font-mono text-[12px] font-semibold uppercase tracking-[0.12em] text-white transition-colors hover:border-white hover:bg-white/10"
        >
          Start free audit
        </a>
        <a
          href="https://app.geoready.dev/signup?plan=pro"
          data-plan-id="pro"
          data-plan-name="Pro"
          data-plan-price="$19"
          data-plan-period="/month"
          data-plan-currency="USD"
          data-cta-location="pricing_footer"
          class="inline-block rounded-[4px] bg-white px-6 py-2.5 font-mono text-[12px] font-semibold uppercase tracking-[0.12em] text-pass-deep transition-colors hover:bg-pass-wash"
        >
          Start with Pro
        </a>
      </div>
      <p class="mt-6 font-mono text-[11px] uppercase tracking-[0.14em] text-pass-wash/80">
        Founding price: open
      </p>
    </div>
  </section>

  {/* Exit micro-survey — 1 domanda, solo evento GA4 (geo_pricing_objection), nessun backend */}
  <section class="bg-paper" aria-label="Pricing feedback">
    <div class="mx-auto max-w-7xl px-4 py-10 sm:px-6">
      <PricingObjectionSurvey client:visible />
    </div>
  </section>

  <script>
    const pricingParams = new URLSearchParams(window.location.search);
    const reportClaim = pricingParams.get('claim');
    const claimSource = pricingParams.get('claim_source') || 'pricing_page';

    if (reportClaim) {
      document.querySelectorAll<HTMLAnchorElement>('a[href^="https://app.geoready.dev/signup"]').forEach((link) => {
        const url = new URL(link.href);
        url.searchParams.set('claim', reportClaim);
        url.searchParams.set('claim_source', claimSource);
        url.searchParams.set('onboarding', 'claim_report');
        link.href = url.toString();
      });
    }
  </script>

  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "WebPage",
        "name": "GeoReady Pricing",
        "description": "GeoReady pricing — free audit with no account required, Pro monitoring, Studio multi-site, Agency workspace.",
        "url": "https://geoready.dev/pricing/",
        "isPartOf": { "@id": "https://geoready.dev/#website" }
      },
      {
        "@type": "ItemList",
        "name": "GeoReady Plans",
        "itemListElement": [
          {
            "@type": "ListItem",
            "position": 1,
            "item": {
              "@type": "Offer",
              "name": "Free",
              "price": "0",
              "priceCurrency": "USD",
              "description": "Single-URL GEO audit, no account required, full 8-category report.",
              "availability": "https://schema.org/InStock",
              "url": "https://geoready.dev"
            }
          },
          {
            "@type": "ListItem",
            "position": 2,
            "item": {
              "@type": "Offer",
              "name": "Pro",
              "price": "19",
              "priceCurrency": "USD",
              "priceSpecification": {
                "@type": "UnitPriceSpecification",
                "price": "19",
                "priceCurrency": "USD",
                "unitCode": "MON"
              },
              "description": "Weekly monitoring for 1 domain, score history, regression alerts, PDF export, API access.",
              "availability": "https://schema.org/InStock",
              "url": "https://app.geoready.dev/signup?plan=pro"
            }
          },
          {
            "@type": "ListItem",
            "position": 3,
            "item": {
              "@type": "Offer",
              "name": "Studio",
              "price": "49",
              "priceCurrency": "USD",
              "priceSpecification": {
                "@type": "UnitPriceSpecification",
                "price": "49",
                "priceCurrency": "USD",
                "unitCode": "MON"
              },
              "description": "15 domains, daily monitoring, unbranded PDF export, competitor comparison, 12-month history.",
              "availability": "https://schema.org/InStock",
              "url": "https://app.geoready.dev/signup?plan=studio"
            }
          },
          {
            "@type": "ListItem",
            "position": 4,
            "item": {
              "@type": "Offer",
              "name": "Agency",
              "price": "89",
              "priceCurrency": "USD",
              "priceSpecification": {
                "@type": "UnitPriceSpecification",
                "price": "89",
                "priceCurrency": "USD",
                "unitCode": "MON"
              },
              "description": "50 domains, daily monitoring, team workspace, unlimited bulk audits, AI answer snapshots (Perplexity automated).",
              "availability": "https://schema.org/InStock",
              "url": "https://app.geoready.dev/signup?plan=agency"
            }
          }
        ]
      },
      {
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "Is the free audit really free?",
            "acceptedAnswer": { "@type": "Answer", "text": "Yes. The web audit requires no account, no credit card, and no signup. You get a full GEO score and the complete 8-category breakdown immediately. The CLI is MIT-licensed and free to use without limits." }
          },
          {
            "@type": "Question",
            "name": "Do I need to register to use the free audit?",
            "acceptedAnswer": { "@type": "Answer", "text": "No. Just enter a URL and run the audit. No email is required to see your results." }
          },
          {
            "@type": "Question",
            "name": "What does Pro add that Free does not?",
            "acceptedAnswer": { "@type": "Answer", "text": "Pro adds continuity, not more report. The free audit is a one-shot snapshot with all 8 categories. Pro monitors your domain weekly, tracks score history over time, and sends alerts when your GEO score drops. It also includes PDF export and API access." }
          },
          {
            "@type": "Question",
            "name": "Are paid plans available right now?",
            "acceptedAnswer": { "@type": "Answer", "text": "Yes. Pick a plan, create an account, and pay — your subscription is active immediately. No sales call, no waitlist. Payments are processed securely by Stripe." }
          },
          {
            "@type": "Question",
            "name": "What happens after I choose a paid plan?",
            "acceptedAnswer": { "@type": "Answer", "text": "You complete Stripe checkout, return to the GeoReady app, and the selected plan activates immediately. If you came from a free report, the claim link can attach that report to your account; otherwise you add a domain from the dashboard and monitoring starts from that point." }
          },
          {
            "@type": "Question",
            "name": "What is the founding price?",
            "acceptedAnswer": { "@type": "Answer", "text": "The current prices are a founding rate for early members. They are locked for the life of the subscription as long as it stays active. The public list price will be higher later." }
          },
          {
            "@type": "Question",
            "name": "Will the CLI remain open source?",
            "acceptedAnswer": { "@type": "Answer", "text": "Yes. The CLI (geo-optimizer-skill on PyPI) is MIT-licensed and will remain free forever. SaaS plans add server-side continuity features the CLI cannot provide on its own." }
          },
          {
            "@type": "Question",
            "name": "Is there a free trial for paid plans?",
            "acceptedAnswer": { "@type": "Answer", "text": "There is no time-limited trial. The Free plan is the permanent baseline — it returns real audit results without commitment. Paid plans add monitoring and team features on top." }
          },
          {
            "@type": "Question",
            "name": "Can I cancel at any time?",
            "acceptedAnswer": { "@type": "Answer", "text": "Yes. You can cancel from your account settings at any time, with no lock-in or penalties." }
          }
        ]
      }
    ]
  })} />
</Shell>
