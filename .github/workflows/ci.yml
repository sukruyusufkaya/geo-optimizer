name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Cancella run precedenti sullo stesso branch/PR per risparmiare minuti CI
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.11', '3.12', '3.13']

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,web]"

      - name: Run tests
        run: |
          pytest tests/ -v --tb=short --cov=geo_optimizer --cov-report=term-missing

      - name: Verify CLI entry point
        run: |
          geo --version
          geo --help

      - name: Test install script (bash check)
        run: |
          bash -n install.sh
          bash -n update.sh

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip

      - name: Install linting tools
        run: pip install ruff pip-audit mypy

      - name: Run ruff
        run: |
          ruff check src/geo_optimizer/
          ruff format --check src/geo_optimizer/

      - name: Run mypy
        run: mypy src/geo_optimizer/

      - name: Audit dependencies
        run: |
          python -m pip install --upgrade pip  # clears PYSEC-2026-196 (runner pip, not a runtime dep)
          pip install .
          pip-audit --ignore-vuln CVE-2026-4539 --ignore-vuln CVE-2026-3219 --ignore-vuln CVE-2026-6357  # pygments / pip: runner-level packages, not runtime deps
